import styles from './order-card.module.css';
import React, { FC } from 'react';
//--------------------------------------------------------------------------------

const OrderCard: FC = () => {
  return <li className={styles.orderCard}></li>;
};

export default OrderCard;
